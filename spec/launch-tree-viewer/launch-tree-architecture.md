# Launch File Tree Viewer - Architecture Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         VS Code IDE                                  │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                     Extension Host                             │  │
│  │  ┌─────────────────────────────────────────────────────────┐  │  │
│  │  │            ROS 2 Extension (TypeScript)                  │  │  │
│  │  │                                                          │  │  │
│  │  │  ┌────────────────────────────────────────────────────┐ │  │  │
│  │  │  │      LaunchTreeDataProvider                         │ │  │  │
│  │  │  │  - implements TreeDataProvider<LaunchTreeItem>     │ │  │  │
│  │  │  │  - manages tree state and updates                  │ │  │  │
│  │  │  │  - handles refresh events                          │ │  │  │
│  │  │  └────────────────┬───────────────────────────────────┘ │  │  │
│  │  │                   │                                      │  │  │
│  │  │                   │ creates                              │  │  │
│  │  │                   ▼                                      │  │  │
│  │  │  ┌────────────────────────────────────────────────────┐ │  │  │
│  │  │  │         LaunchTreeItem                              │ │  │  │
│  │  │  │  - extends TreeItem                                │ │  │  │
│  │  │  │  - represents nodes in tree                        │ │  │  │
│  │  │  │  - handles icons, labels, tooltips                 │ │  │  │
│  │  │  └────────────────────────────────────────────────────┘ │  │  │
│  │  │                                                          │  │  │
│  │  │  ┌────────────────────────────────────────────────────┐ │  │  │
│  │  │  │         LaunchFileParser                            │ │  │  │
│  │  │  │  - findWorkspacePackages()                         │ │  │  │
│  │  │  │  - parseLaunchFile()                               │ │  │  │
│  │  │  │  - manages cache                                   │ │  │  │
│  │  │  └────────────────┬───────────────────────────────────┘ │  │  │
│  │  │                   │                                      │  │  │
│  │  │                   │ executes                             │  │  │
│  │  │                   ▼                                      │  │  │
│  │  │  ┌────────────────────────────────────────────────────┐ │  │  │
│  │  │  │    Command Handlers                                 │ │  │  │
│  │  │  │  - refresh()                                        │ │  │  │
│  │  │  │  - reveal()                                         │ │  │  │
│  │  │  │  - findUsages()                                     │ │  │  │
│  │  │  │  - run() / debug()                                  │ │  │  │
│  │  │  └────────────────────────────────────────────────────┘ │  │  │
│  │  └──────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                  VS Code UI Layer                              │  │
│  │  ┌─────────────────┬──────────────────┬──────────────────┐   │  │
│  │  │  Activity Bar   │  Sidebar         │  Editor Area     │   │  │
│  │  │  ┌───────────┐  │  ┌────────────┐  │  ┌────────────┐  │   │  │
│  │  │  │ 📁 Files  │  │  │ 🌲 Tree    │  │  │ launch.py  │  │   │  │
│  │  │  │ 🔍 Search │  │  │  View      │  │  │            │  │   │  │
│  │  │  │ 🌿 Git    │  │  │  �� pkg    │  │  │  def gen() │  │   │  │
│  │  │  │ 🐛 Debug  │  │  │   🚀 .py   │  │  │    Node()  │  │   │  │
│  │  │  │ 🤖 ROS 2  │  │  │    📍 node │  │  │            │  │   │  │
│  │  │  │   (opt)   │  │  │     📊 ⚙️  │  │  │            │  │   │  │
│  │  │  └───────────┘  │  └────────────┘  │  └────────────┘  │   │  │
│  │  └─────────────────┴──────────────────┴──────────────────┘   │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ spawns process
                              ▼
          ┌──────────────────────────────────────────┐
          │   Python Subprocess (ROS 2 Environment)  │
          │  ┌────────────────────────────────────┐  │
          │  │   ros2_launch_dumper.py            │  │
          │  │   - Parses .launch.py files        │  │
          │  │   - Extracts nodes, includes       │  │
          │  │   - Returns JSON data              │  │
          │  └────────────────────────────────────┘  │
          └──────────────────────────────────────────┘
                              │
                              │ returns JSON
                              ▼
          ┌──────────────────────────────────────────┐
          │        Launch File Data (JSON)           │
          │  {                                        │
          │    "processes": [...],                   │
          │    "lifecycle_nodes": [...],             │
          │    "includes": [...],                    │
          │    "arguments": [...],                   │
          │    "warnings": [...],                    │
          │    "errors": [...]                       │
          │  }                                        │
          └──────────────────────────────────────────┘
```

## Data Flow

### 1. Tree Initialization

```
User Opens Workspace
       │
       ▼
LaunchTreeDataProvider.getRootItems()
       │
       ├─> LaunchFileParser.findWorkspacePackages()
       │        │
       │        ├─> Find all package.xml files
       │        ├─> Read package names
       │        └─> Find *.launch.py in each package
       │
       └─> Create LaunchTreeItem[] for packages
                │
                └─> VS Code renders tree
```

### 2. Launch File Expansion

```
User Clicks Launch File
       │
       ▼
LaunchTreeDataProvider.getChildren(launchFileItem)
       │
       ├─> LaunchFileParser.parseLaunchFile(path)
       │        │
       │        ├─> Check cache
       │        │
       │        ├─> Execute ros2_launch_dumper.py
       │        │        │
       │        │        └─> Python parses launch file
       │        │                 │
       │        │                 └─> Returns JSON
       │        │
       │        └─> Cache result
       │
       ├─> Extract nodes, includes, arguments
       │
       └─> Create LaunchTreeItem[] for each
                │
                └─> VS Code renders children
```

### 3. Node Expansion

```
User Clicks Node
       │
       ▼
LaunchTreeDataProvider.getChildren(nodeItem)
       │
       ├─> Extract node metadata from cached data
       │        │
       │        ├─> Package name
       │        ├─> Executable
       │        ├─> Namespace
       │        ├─> Parameters (key-value pairs)
       │        ├─> Remappings (from -> to)
       │        └─> Arguments (list)
       │
       └─> Create LaunchTreeItem[] for each property
                │
                └─> VS Code renders details
```

### 4. File Navigation

```
User Clicks Launch File Item
       │
       ▼
LaunchTreeItem.command (vscode.open)
       │
       └─> VS Code opens file in editor
```

### 5. Context Menu Actions

```
User Right-Clicks Launch File
       │
       ├─> "Run Launch File"
       │        │
       │        └─> ROS2.roslaunch command
       │
       ├─> "Debug Launch File"
       │        │
       │        └─> Start debug session
       │
       └─> "Find Usages"
                │
                └─> Search workspace for includes
```

## Class Diagram

```
┌────────────────────────────────────┐
│  vscode.TreeDataProvider<T>       │
│  (VS Code API Interface)           │
└────────────────┬───────────────────┘
                 │
                 │ implements
                 │
┌────────────────▼───────────────────┐
│  LaunchTreeDataProvider            │
├────────────────────────────────────┤
│ - parser: LaunchFileParser         │
│ - workspacePackages: IWorkspace[]  │
│ - fileWatcher: FileSystemWatcher   │
├────────────────────────────────────┤
│ + getTreeItem(element): TreeItem   │
│ + getChildren(element): TreeItem[] │
│ + refresh(): void                  │
│ - getRootItems(): TreeItem[]       │
│ - getPackageLaunchFiles(): ...     │
│ - getLaunchFileContents(): ...     │
│ - getNodeDetails(): ...            │
└────────────────┬───────────────────┘
                 │
                 │ creates
                 │
┌────────────────▼───────────────────┐
│  vscode.TreeItem                   │
│  (VS Code API Class)               │
└────────────────┬───────────────────┘
                 │
                 │ extends
                 │
┌────────────────▼───────────────────┐
│  LaunchTreeItem                    │
├────────────────────────────────────┤
│ + type: LaunchTreeItemType         │
│ + packageName?: string             │
│ + launchFilePath?: string          │
│ + nodeName?: string                │
│ + parameters?: Record<string, any> │
│ + remappings?: [string, string][]  │
├────────────────────────────────────┤
│ + static createPackageItem()       │
│ + static createLaunchFileItem()    │
│ + static createNodeItem()          │
│ + static createParameterItem()     │
└────────────────────────────────────┘

┌────────────────────────────────────┐
│  LaunchFileParser                  │
├────────────────────────────────────┤
│ - dumperScript: string             │
│ - cache: Map<string, ILaunchData>  │
├────────────────────────────────────┤
│ + findWorkspacePackages(): ...     │
│ + parseLaunchFile(): ILaunchData   │
│ + invalidateCache(path): void      │
│ + clearCache(): void               │
│ - findPackagesInPath(): ...        │
│ - findLaunchFilesInPackage(): ...  │
│ - getPackageNameFromXml(): ...     │
└────────────────────────────────────┘
```

## Sequence Diagram: Refresh Tree

```
User         VS Code      TreeProvider    Parser       Dumper.py
 │              │              │             │              │
 │──click refresh──>           │             │              │
 │              │              │             │              │
 │              │──refresh()──>│             │              │
 │              │              │             │              │
 │              │              │──findPackages()──>        │
 │              │              │             │              │
 │              │              │<────packages[]─────        │
 │              │              │             │              │
 │              │              │──parseLaunchFile()──>     │
 │              │              │             │              │
 │              │              │             │──execute()──>│
 │              │              │             │              │
 │              │              │             │<──JSON───────│
 │              │              │             │              │
 │              │              │<────ILaunchData───         │
 │              │              │             │              │
 │              │<─TreeItems[]─│             │              │
 │              │              │             │              │
 │<─render tree─│              │             │              │
 │              │              │             │              │
```

## State Management

```
┌──────────────────────────────────────┐
│         Application State             │
├──────────────────────────────────────┤
│                                       │
│  ┌────────────────────────────────┐  │
│  │   Workspace Packages            │  │
│  │   - name: string                │  │
│  │   - path: string                │  │
│  │   - launchFiles: string[]       │  │
│  └────────────────────────────────┘  │
│                                       │
│  ┌────────────────────────────────┐  │
│  │   Parse Cache                   │  │
│  │   Map<filePath, ILaunchData>    │  │
│  │   - processes                   │  │
│  │   - lifecycle_nodes             │  │
│  │   - includes                    │  │
│  │   - arguments                   │  │
│  └────────────────────────────────┘  │
│                                       │
│  ┌────────────────────────────────┐  │
│  │   Tree State                    │  │
│  │   - expanded nodes              │  │
│  │   - selected item               │  │
│  │   - scroll position             │  │
│  └────────────────────────────────┘  │
│                                       │
└──────────────────────────────────────┘
```

## File System Watcher

```
File System Changes
       │
       ├─> *.launch.py created
       │        │
       │        ├─> Debounce (500ms)
       │        │
       │        └─> refresh()
       │
       ├─> *.launch.py modified
       │        │
       │        ├─> Invalidate cache
       │        │
       │        ├─> Debounce (500ms)
       │        │
       │        └─> refresh()
       │
       └─> *.launch.py deleted
                │
                ├─> Invalidate cache
                │
                ├─> Debounce (500ms)
                │
                └─> refresh()
```

## Extension Lifecycle

```
┌─────────────────────────────────────┐
│  VS Code Activation                 │
│  (workspace contains *.launch.py)   │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  Extension.activate()               │
├─────────────────────────────────────┤
│  1. Create LaunchTreeDataProvider   │
│  2. Register tree view               │
│  3. Register commands                │
│  4. Setup file watcher               │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  Tree Provider Active                │
│  - Listening for file changes        │
│  - Responding to user interactions   │
│  - Caching parse results             │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  Extension.deactivate()             │
├─────────────────────────────────────┤
│  1. Dispose file watcher             │
│  2. Clear cache                      │
│  3. Dispose tree view                │
└─────────────────────────────────────┘
```

## Integration Points

```
┌────────────────────────────────────────────────────────┐
│              ROS 2 Extension Ecosystem                  │
├────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────────────┐    ┌─────────────────────┐   │
│  │  Launch Tree View   │───>│  ros2_launch_dumper │   │
│  └─────────────────────┘    └─────────────────────┘   │
│            │                                            │
│            │ uses                                       │
│            ▼                                            │
│  ┌─────────────────────┐                               │
│  │  Debug Adapter      │                               │
│  │  (ros2 debugger)    │                               │
│  └─────────────────────┘                               │
│            │                                            │
│            │ integrates                                 │
│            ▼                                            │
│  ┌─────────────────────┐    ┌─────────────────────┐   │
│  │  ROS2.roslaunch     │    │  ROS2.lifecycle     │   │
│  │  (run command)      │    │  (node management)  │   │
│  └─────────────────────┘    └─────────────────────┘   │
│            │                          │                │
│            │                          │                │
│            └────────┬─────────────────┘                │
│                     │                                  │
│                     ▼                                  │
│  ┌──────────────────────────────────────────────────┐ │
│  │         ROS 2 Environment (Python/C++)           │ │
│  └──────────────────────────────────────────────────┘ │
│                                                         │
└────────────────────────────────────────────────────────┘
```

---

**Legend:**
- `─>` : Data flow / calls
- `│` : Hierarchy / ownership
- `▼` : Process flow
- `[]` : Array / collection
- `{}` : Object / dictionary

**Author:** GitHub Copilot  
**Date:** 2026-01-31  
**Version:** 1.0
